import {
	CHANGE_FILTER_TYPE
} from 'actionTypes';

const initialState = 'id';

export default (state = initialState, {type, payload}) => {
	switch (type) {
		case CHANGE_FILTER_TYPE:
			return payload;
		default:
			return state;
	}
}