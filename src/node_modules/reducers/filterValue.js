import {
    CHANGE_FILTER_VALUE
} from 'actionTypes';

const initialState = '';

export default (state = initialState, {type, payload}) => {
    switch (type) {
        case CHANGE_FILTER_VALUE:
            return payload;
        default:
            return state;
    }
}