import {
	CHANGE_SORT_VALUE
} from 'actionTypes';

const initialState = 'id';

export default (state = initialState, {type, payload}) => {
	switch (type) {
		case CHANGE_SORT_VALUE:
			return payload;
		default:
			return state;
	}
}