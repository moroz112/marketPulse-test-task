import {
	UPDATE_ASSETS
} from 'actionTypes';

const initialState = [];

export default (state = initialState, {type, payload}) => {
	switch (type) {
		case UPDATE_ASSETS:
			return payload;
		default:
			return state;
	}
}